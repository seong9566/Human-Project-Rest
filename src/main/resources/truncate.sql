INSERT INTO users (login_id, login_password, users_id, created_at)
VALUES('user2', '1234', 1, now());
-- SET REFERENTIAL_INTEGRITY FALSE;
-- truncate table alarm;
-- truncate table apply;
-- truncate table career;
-- truncate table category;
-- truncate table company;
-- truncate table company_like;
-- truncate table job_posting_board;
-- truncate table personal;
-- truncate table personal_like;
-- truncate table portfolio;
-- truncate table resumes;
-- truncate table subscribe;
-- truncate table users;
-- SET REFERENTIAL_INTEGRITY TRUE;